#programa para conferir se é vogal ou consoante
vogal = ['a','e', 'i', 'o', 'u']

for letra in vogal:
    while True:
        letra = input("digita uma letra")
        if letra in vogal:
            print ("é vogal")
        else:
            print ("é consoante")
