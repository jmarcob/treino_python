#indicando quantas raizes tem a equação do segundo grau
a = eval(input("insira o índice a: "))
b = eval(input("insira o índice b: "))
c = eval(input("insira o índice c: "))
delta = ((b**2)-4*a*c)
if delta < 0:
    print ("A equação não possui raiz real")
elif delta == 0:
    print ("a equação possui apenas uma raiz real")
else:
    print("a equação possui duas raizes reais")
